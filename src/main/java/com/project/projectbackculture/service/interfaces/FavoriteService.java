package com.project.projectbackculture.service.interfaces;

import com.project.projectbackculture.persistence.model.FavoriteModel;


public interface FavoriteService extends ServiceGeneral<FavoriteModel, Integer> {

    // no hay mas metodos para  favorite model
}
